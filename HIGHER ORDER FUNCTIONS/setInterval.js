//setInterval => a piece of code which will keep on executing again and again following the seconds mentioned inside it.

setInterval(() => {
    console.log("i'll come everytime a second has passed");
}, 1000);