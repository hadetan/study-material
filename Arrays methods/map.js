//map => this method is used to apply any condition or method into each and every value inside the array. it'll be applied to the whole array.

let arr = [1,4,9,16,25];
console.log(arr.map(Math.sqrt));