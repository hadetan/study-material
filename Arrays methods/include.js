//includes => its a method to check wether a value inside the array is at that index or not. exp: [1,2,3,4,5,6,7] 3 (value inside the array) & 2 (index of that value in the array). Suppose if this is correct then it'll print "true".

let num = [1,2,3,4,5,6,7,8,9];
console.log(num.includes(3, 2));